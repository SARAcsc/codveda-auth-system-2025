## 🚀 Run Locally

1. Install MongoDB (or run `docker run -d -p 27017:27017 mongo`)
2. `cd backend`
3. `npm install`
4. `npm run dev`
5. Test with Thunder Client at `http://localhost:5000`

## 🖥️ Frontend Demo
1. Start backend: `cd backend && npm run dev`
2. Open `frontend/index.html` in browser (use Live Server)
3. Register, login, and view your profile!